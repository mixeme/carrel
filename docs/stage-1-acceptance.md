# Приёмка этапа 1

> **Устарело — к удалению.** Реализация завершена в **v0.1.0**. Актуальный чеклист открытых пунктов: [stage-1-outstanding.md](stage-1-outstanding.md). Этот файл — архив приёмки.

Список §21 [carrel-spec.md](carrel-spec.md), отфильтрованный по границе этапа 1: разделы
«Пользователи и безопасность», «Депонирование», «Развёртывание» и «Лицензия». Разделы
«Подключение и данные», «Фотографии», «Единый вид и дубликаты», «Опрос и деградация» и
«Кэш» относятся к этапам 2–5 и здесь не проверяются.

Каждый пункт помечен тем, чем он закрыт:

- **auto** — есть автотест, имя приведено рядом; проверяется командой `go test ./...`
- **manual** — требует запуска контейнера или чтения глазами, порядок действий ниже

---

## Автоматическая часть

```
go test ./...
go test -race ./...     # требует CGO_ENABLED=1 и установленного C-компилятора
```

Оба прогона должны пройти без падений. Детектор гонок обязателен: сессии, очередь писем
и ограничитель частоты работают из нескольких горутин. Крипто-профили в тестах занижены
намеренно (`newApp` в `internal/web/handler`), сами параметры Argon2id проверяются в
`internal/crypto`.

### Пользователи и безопасность

| Критерий §21 | Чем закрыт |
|---|---|
| Администратор не видит данных других пользователей при выключенном депонировании | auto — `TestAdminCannotReadAnotherAccountsData` |
| Приглашённый заводит пароль сам; до первого входа нет ни хэша, ни DEK | auto — `TestStageOneAcceptanceFlow` |
| Ссылка приглашения работает при полностью ненастроенном SMTP | auto — `TestStageOneAcceptanceFlow`, `TestInviteWorksWithoutSMTP` |
| Токен после использования недействителен; в хранилище только хэш | auto — `TestStageOneAcceptanceFlow`, `TestInviteStoresOnlyDigest`, `TestHashToken` |
| Тестовое письмо показывает диагностику при неверных настройках SMTP | auto — `TestSendPassesTheServersRefusalThrough`, `TestSendReportsAnUnreachableRelay` (+ manual A6) |
| Сброс пароля админом делает подключения недоступными, о чём он предупреждён до подтверждения | auto — `TestResetPasswordIsAnnouncedAsDestructive`, `TestResetPasswordReplacesDEK` |
| Смена пароля пользователем сохраняет все подключения | auto — `TestChangePasswordKeepsDEK`, `TestProfilePasswordChange` |
| Отключение пользователя немедленно завершает его сессии | auto — `TestDisableEndsActiveSessionsAtOnce` |
| Удаление последнего администратора невозможно | auto — `TestLastAdminSurvivesThePanel`, `TestLastAdminGuard` |
| На экране входа нет вводящего в заблуждение восстановления пароля | auto — `TestForgotOffersNoReset` |

### Депонирование

| Критерий §21 | Чем закрыт |
|---|---|
| При выключенной схеме нет ни одной копии DEK, доступной кому-либо кроме владельца | auto — `TestEscrowOffByDefault` (store и handler), `TestAdminCannotReadAnotherAccountsData` |
| Включение не даёт доступа к учётным записям, созданным до включения | auto — `TestEscrowAppliesOnlyToLaterUsers`, `TestRecoveryRefusedWithoutADeposit` |
| Восстановление невозможно без мастер-пароля, отличного от пароля админа | auto — `TestRecoveryNeedsTheMasterPassword`, `TestRecoveryIsThrottled` |
| Каждое восстановление отражено в журнале и в письме пользователю | auto — `TestRecoveryThroughThePanel`, `TestEscrowActionsAreAudited` (+ manual A7 для самого письма) |
| Профиль показывает, действует ли депонирование | auto — `TestEscrowCoversNewAccountsAndSaysSo`, `TestForbiddenOptOutIsVisible` |

### Лицензия

| Критерий §21 | Чем закрыт |
|---|---|
| Страница «О сервисе» доступна без входа, показывает версию, коммит и ссылку на исходники | auto — `TestAboutPublic`, `TestAboutNoSessionRequired` |
| Версия и коммит совпадают с зашитыми при сборке | manual — A2 |
| `THIRD_PARTY.md` содержит лицензии всех зависимостей и вендоренного htmx | manual — A9 |

### Развёртывание

Целиком manual: A1, A3, A4, A5. Пункт «прогресс веерного опроса через Apache» относится
к этапу 5 и здесь не проверяется.

---

## Ручная часть

Сборка с настоящими метаданными — они же проверяются в A2:

```
docker compose build --build-arg VERSION=$(git describe --tags --always) --build-arg COMMIT=$(git rev-parse --short HEAD)
```

### A1. Пустой том → экран создания администратора

```
docker volume rm carrel_carrel-data   # если остался от прошлого прогона
docker compose up -d
```

- [ ] `http://127.0.0.1:8080/` перенаправляет на `/setup`
- [ ] Форма создаёт первого администратора и сразу открывает панель
- [ ] Повторное открытие `/setup` ведёт на `/login`, второго администратора завести нельзя
- [ ] `docker compose ps` показывает состояние `healthy` (healthcheck без shell, через `carrel healthcheck`)

### A2. Версия и коммит

- [ ] `/about` открывается без входа
- [ ] Версия и коммит на странице совпадают с аргументами сборки из команды выше
- [ ] Ссылка на исходники ведёт на репозиторий, лицензия названа AGPL-3.0-or-later

### A3. `read_only: true` и непривилегированный пользователь

При работающем контейнере:

```
docker compose exec carrel /carrel healthcheck   # distroless: без shell, только бинарь
docker inspect --format '{{.HostConfig.ReadonlyRootfs}} {{.Config.User}}' <container>
```

- [ ] `ReadonlyRootfs` = `true`, пользователь непривилегированный (`nonroot`, uid 65532)
- [ ] Создание администратора и правка настроек проходят при read-only корне: пишется только том `/var/lib/carrel`
- [ ] `docker inspect --format '{{.HostConfig.CapDrop}}'` показывает `[ALL]`
- [ ] Порт слушается только на петле: `curl` с адреса хоста в сети работает, снаружи — нет

### A4. SIGTERM не рвёт активные запросы

```
docker compose stop -t 20 carrel
```

- [ ] Запрос, выполнявшийся в момент остановки, завершается штатно, а не обрывом соединения
- [ ] В логах есть запись о завершении, процесс уходит до истечения 15 секунд, если запросов нет
- [ ] После перезапуска все сессии сброшены — вход требуется заново (ключи живут только в памяти)

### A5. Логи не содержат секретов

Добавить в `compose.yaml` на время проверки `CARREL_LOG_LEVEL: debug` рядом с
`CARREL_DATA_DIR` и перезапустить:

```
docker compose up -d
docker compose logs carrel > /tmp/carrel.log
```

Пройти вход, создание приглашения, его приём, смену пароля и тестовое письмо, затем:

- [ ] В `/tmp/carrel.log` нет паролей, токенов приглашений, мастер-пароля и содержимого писем
- [ ] Логи структурированные (JSON), идут в stdout

### A6. Диагностика SMTP

- [ ] С заведомо неверным хостом «Send test email» показывает полный ответ (отказ соединения), а не «ошибка»
- [ ] С неверным паролем показан ответ сервера на аутентификацию
- [ ] В выводе диагностики нет пароля реле
- [ ] С верными настройками письмо доходит, в диагностике виден весь диалог

### A7. Письмо о восстановлении

- [ ] Включить депонирование, создать пользователя, восстановить его — пользователю уходит письмо
- [ ] Письмо нельзя отключить из панели
- [ ] При ненастроенном SMTP или отсутствии адреса панель сообщает, что уведомить надо самому

### A8. Приглашение без SMTP (сквозной проход руками)

- [ ] SMTP не настроен: приглашение создаётся, ссылка показана с кнопкой копирования
- [ ] Статус доставки — `not configured`, приглашение при этом действительно
- [ ] По ссылке пользователь задаёт пароль и попадает внутрь
- [ ] Повторный переход по той же ссылке отдаёт «недействительно»

### A9. `THIRD_PARTY.md`

- [ ] Перечислены все зависимости из `go.mod` и вендоренный htmx с его SSE-расширением
- [ ] Для каждой приведён текст лицензии, версии совпадают с `go.mod` и с файлами в `internal/web/static/`
