# Contract of contact_crop.html — frozen before the rebuild (docs/plans/interface-rebuild.md §4).
#
# Every line is a name something outside the markup depends on: a form field,
# an htmx or SSE binding, a data-* a script hooks, an id another node aims at,
# or a field of the view this template reads. The template rewritten from its
# frame has to bring each one back.
#
# A name may be dropped — but only out loud. Put "- " in front of the line and
# the reason after "#":
#
#     - field:sort_order  # the bar sorts by link now, not by a posted field
#
# Then the lost behaviour is a decision visible in the diff instead of a side
# effect of retyping the screen.
#
# Regenerate with: go test ./internal/web/handler -run TestScreenKeepsItsContract -update
bind:hx-post=*/app/contacts/*/*/*
bind:hx-swap=outerHTML
bind:hx-target=#photo-crop
data:data-crop-canvas
data:data-crop-form
data:data-crop-frame
data:data-crop-img
data:data-crop-num=pan_x
data:data-crop-num=pan_y
data:data-crop-num=rotate
data:data-crop-num=zoom
data:data-crop-pan-x
data:data-crop-pan-y
data:data-crop-result
data:data-crop-rotate
data:data-crop-zoom
field:action
field:csrf_token
field:etag
field:pan_x
field:pan_y
field:rotate
field:type=hidden
field:type=number
field:zoom
form:action=*/app/contacts/*/*/*
form:method=post
id:photo-crop
read:AccountID
read:Base
read:CSRF
read:ColEnc
read:Crop
read:Crop.PanX
read:Crop.PanY
read:Crop.Preview
read:Crop.Rotate
read:Crop.Zoom
read:Data
read:ETag
read:UID
